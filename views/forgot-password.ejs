<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lupa Password - AnimeStream</title>
  <style>
    body { margin: 0; font-family: Arial, sans-serif; }
    .page { display: flex; min-height: 100vh; background: linear-gradient(135deg, #00a1d6, #00b5e5); align-items: center; justify-content: center; padding: 2rem; }
    .box { background: white; border-radius: 16px; padding: 3rem; max-width: 500px; width: 100%; box-shadow: 0 10px 40px rgba(0,0,0,0.1); }
    h2 { color: #333; text-align: center; margin-bottom: 0.5rem; }
    .subtitle { color: #999; text-align: center; margin-bottom: 2rem; }
    .form-group { margin-bottom: 1.5rem; }
    label { display: block; margin-bottom: 0.5rem; color: #333; font-weight: 500; }
    input { width: 100%; padding: 0.9rem; border: 1px solid #e5e5e5; border-radius: 8px; box-sizing: border-box; font-size: 1rem; }
    input:focus { outline: none; border-color: #00a1d6; box-shadow: 0 0 0 3px rgba(0, 161, 214, 0.1); }
    input[name="code"] { font-size: 1.5rem; text-align: center; letter-spacing: 8px; font-family: monospace; font-weight: bold; }
    button { width: 100%; padding: 1rem; background: #00a1d6; color: white; border: none; border-radius: 8px; font-size: 1rem; font-weight: 600; cursor: pointer; }
    button:hover { background: #00b5e5; }
    .link { text-align: center; margin-top: 1.5rem; }
    .link a { color: #00a1d6; text-decoration: none; }
    .error { padding: 1rem; background: #fff5f5; border: 1px solid #ffe5e5; color: #ff6b6b; border-radius: 8px; margin-bottom: 1.5rem; }
    .success { padding: 1rem; background: #f0fdf4; border: 1px solid #bbf7d0; color: #15803d; border-radius: 8px; margin-bottom: 1.5rem; }
    .code-box { background: linear-gradient(135deg, #00a1d6, #00b5e5); padding: 2rem; border-radius: 12px; text-align: center; margin: 1.5rem 0; color: white; }
    .code { font-size: 3rem; font-weight: bold; letter-spacing: 10px; font-family: monospace; text-shadow: 2px 2px 4px rgba(0,0,0,0.2); }
    .info { background: #f0f9ff; border: 1px solid #bfdbfe; padding: 1rem; border-radius: 8px; margin-bottom: 1.5rem; color: #1e40af; font-size: 0.9rem; }
  </style>
</head>
<body>
  <div class="page">
    <div class="box">
      <% if (!success) { %>
        <h2>Lupa Password?</h2>
        <p class="subtitle">Masukkan email untuk mendapatkan kode reset</p>
        <% if (error) { %>
          <div class="error"><%= error %></div>
        <% } %>
        <form action="/forgot-password" method="POST">
          <div class="form-group">
            <label>Email</label>
            <input type="email" name="email" required placeholder="Masukkan email kamu">
          </div>
          <button type="submit">Dapatkan Kode Reset</button>
        </form>
        <div class="link"><a href="/login">Kembali ke Login</a></div>
      <% } else { %>
        <h2>Verifikasi Kode</h2>
        <p class="subtitle">Masukkan kode yang ditampilkan di bawah</p>
        <div class="code-box">
          <div style="font-size: 1.1rem; margin-bottom: 1rem;">Kode Reset Anda:</div>
          <div class="code"><%= resetCode %></div>
          <div style="margin-top: 1rem; opacity: 0.9; font-size: 0.9rem;">Berlaku selama 1 jam</div>
        </div>
        <div class="info">
          Salin kode 6 digit di atas dan masukkan ke form di bawah untuk verifikasi.
        </div>
        <% if (typeof verifyError !== 'undefined' && verifyError) { %>
          <div class="error"><%= verifyError %></div>
        <% } %>
        <form action="/verify-code" method="POST">
          <div class="form-group">
            <label>Masukkan Kode (6 Digit)</label>
            <input type="text" name="code" required placeholder="000000" maxlength="6" pattern="[0-9]{6}" autocomplete="off" autofocus>
          </div>
          <button type="submit">Verifikasi Kode</button>
        </form>
        <div class="link"><a href="/forgot-password">Request Kode Baru</a></div>
      <% } %>
    </div>
  </div>
  <script>
    const codeInput = document.querySelector('input[name="code"]');
    if (codeInput) {
      codeInput.addEventListener('input', function(e) {
        this.value = this.value.replace(/[^0-9]/g, '');
      });
    }
  </script>
</body>
</html>
