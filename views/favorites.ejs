<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#8a2be2">
  <title>Favorit - AnimeStream</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
  <nav class="navbar">
    <div class="container">
      <h1 class="logo">🎬 AnimeStream</h1>
      <div class="nav-right">
        <span class="user-badge">👤 <%= user.username %></span>
        <a href="/logout" class="btn-logout">Keluar</a>
      </div>
    </div>
  </nav>

  <div class="dashboard-container">
    <aside class="sidebar">
      <div class="sidebar-menu">
        <a href="/dashboard" class="menu-item">
          <span>📊</span>
          <span>Dashboard</span>
        </a>
        <a href="/dashboard/my-uploads" class="menu-item">
          <span>📹</span>
          <span>Upload Saya</span>
        </a>
        <a href="/dashboard/favorites" class="menu-item active">
          <span>❤️</span>
          <span>Favorit</span>
        </a>
        <a href="/dashboard/history" class="menu-item">
          <span>🕐</span>
          <span>Riwayat</span>
        </a>
        <% if (user.role === 'admin') { %>
        <a href="/dashboard/admin" class="menu-item">
          <span>⚙️</span>
          <span>Admin Panel</span>
        </a>
        <% } %>
        <a href="/settings" class="menu-item">
          <span>🔧</span>
          <span>Pengaturan</span>
        </a>
      </div>
    </aside>

    <main class="dashboard-content">
      <div class="dashboard-header">
        <h2>❤️ Anime Favorit</h2>
        <p>Koleksi anime favorit yang kamu sukai</p>
      </div>

      <% if (favorites.length === 0) { %>
        <div class="empty-state">
          <p style="font-size: 3rem; margin-bottom: 1rem;">❤️</p>
          <p style="font-size: 1.2rem; color: #aaa;">Belum ada anime favorit</p>
          <p style="font-size: 0.9rem; color: #777; margin-bottom: 2rem;">Tambahkan anime ke favorit saat menonton untuk menyimpannya di sini</p>
          <a href="/" class="btn btn-primary" style="display: inline-block; padding: 0.8rem 2rem; text-decoration: none;">
            🔍 Jelajahi Anime
          </a>
        </div>
      <% } else { %>
        <div style="background: rgba(30, 30, 50, 0.8); backdrop-filter: blur(10px); border-radius: 12px; padding: 1.5rem; border: 2px solid rgba(168, 85, 247, 0.3);">
          <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
            <h3 style="color: #a855f7; margin: 0;">Total Favorit: <%= favorites.length %> Anime</h3>
          </div>

          <div class="anime-grid">
            <% favorites.forEach(anime => { %>
              <div class="anime-card" onclick="window.location.href='/watch/<%= anime.id %>'">
                <div class="anime-thumbnail">
                  <video src="/uploads/<%= anime.videoPath %>" preload="metadata"></video>
                  <span class="anime-badge">❤️</span>
                  <span class="anime-duration">24:00</span>
                  <div class="play-overlay">▶</div>
                </div>
                <div class="anime-info">
                  <h3><%= anime.title %></h3>
                  <div class="anime-meta">
                    <span class="episode">Ep <%= anime.episode %></span>
                    <span>👁️ <%= anime.views || 0 %></span>
                  </div>
                  <% if (anime.description) { %>
                    <p class="description"><%= anime.description %></p>
                  <% } %>
                  <% if (anime.genre) { %>
                    <div class="genre-tags">
                      <% anime.genre.split(',').forEach(g => { %>
                        <span class="genre-tag"><%= g.trim() %></span>
                      <% }); %>
                    </div>
                  <% } %>
                  <% if (anime.uploader) { %>
                    <div class="uploader">
                      <span>👤</span>
                      <span><%= anime.uploader %></span>
                    </div>
                  <% } %>
                  <div class="actions" style="margin-top: 0.8rem;">
                    <a href="/watch/<%= anime.id %>" class="btn btn-primary" onclick="event.stopPropagation()">Tonton</a>
                    <button onclick="event.stopPropagation(); removeFavorite(<%= anime.id %>)" class="btn btn-danger">Hapus</button>
                  </div>
                </div>
              </div>
            <% }); %>
          </div>
        </div>
      <% } %>

      <!-- Coming Soon Notice -->
      <div style="background: rgba(168, 85, 247, 0.1); border: 2px solid rgba(168, 85, 247, 0.3); border-radius: 12px; padding: 1.5rem; margin-top: 2rem;">
        <h3 style="color: #a855f7; margin-bottom: 0.5rem;">🚀 Fitur Segera Hadir</h3>
        <p style="color: #aaa; margin: 0;">Sistem favorit akan segera diimplementasikan. Kamu akan bisa menambahkan anime favorit langsung dari halaman watch!</p>
      </div>
    </main>
  </div>

  <div class="bottom-nav">
    <a href="/">
      <span>🏠</span>
      <span>Beranda</span>
    </a>
    <a href="/dashboard" class="active">
      <span>📊</span>
      <span>Dashboard</span>
    </a>
    <a href="/upload">
      <span>⬆️</span>
      <span>Upload</span>
    </a>
    <a href="/settings">
      <span>👤</span>
      <span>Profil</span>
    </a>
  </div>

  <script>
    function removeFavorite(id) {
      if (confirm('Hapus dari favorit?')) {
        // TODO: Implement remove favorite API
        alert('Fitur ini akan segera tersedia!');
      }
    }
  </script>

  <!-- WhatsApp Floating Button -->
  <!-- WhatsApp Floating Button (Fixed) -->
  <a href="https://wa.me/6282297706541?text=Hai" target="_blank" class="wa-float" style="position: fixed; bottom: 20px; right: 20px; z-index: 99999; width: 56px; height: 56px; border-radius: 50%; background: linear-gradient(135deg, #25D366, #128C7E); color: white; display: flex; align-items: center; justify-content: center; font-size: 26px; text-decoration: none; box-shadow: 0 4px 16px rgba(37, 211, 102, 0.6); transition: all 0.3s; will-change: transform; pointer-events: auto;">
    💬
  </a>

  <style>
    .wa-float {
      pointer-events: auto !important;
    }
    .wa-float:hover {
      transform: scale(1.1) !important;
      box-shadow: 0 6px 20px rgba(37, 211, 102, 0.8) !important;
    }
    .wa-float:active {
      transform: scale(0.95) !important;
    }
    @media (max-width: 768px) {
      .wa-float {
        bottom: 15px !important;
        right: 15px !important;
        width: 50px !important;
        height: 50px !important;
        font-size: 22px !important;
      }
    }
  </style>
</body>
</html>
