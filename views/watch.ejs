<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="theme-color" content="#1a1a2e">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <title><%= anime.title %> - Episode <%= anime.episode %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar">
    <div class="container">
      <h1 class="logo">🎬 AnimeStream</h1>
      <div class="nav-right">
        <% if (user) { %>
          <a href="/dashboard" class="user-badge">👤 <%= user.username %></a>
          <a href="/logout" class="btn-logout" style="margin-left: 1rem; padding: 0.5rem 1rem; background: #ff4757; color: white; border-radius: 8px; text-decoration: none; font-size: 0.9rem;">Keluar</a>
        <% } else { %>
          <a href="/login" class="btn-upload">Masuk</a>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container">
    <div class="watch-container">
      <h2><%= anime.title %> - Episode <%= anime.episode %></h2>
      
      <div class="video-player">
        <% 
        let videoUrl = anime.videoPath;
        let isExternal = videoUrl.startsWith('http://') || videoUrl.startsWith('https://');
        
        // Convert Google Drive link to direct streaming link
        if (isExternal && videoUrl.includes('drive.google.com')) {
          // Extract file ID from Google Drive URL
          let fileId = null;
          if (videoUrl.includes('/file/d/')) {
            fileId = videoUrl.split('/file/d/')[1].split('/')[0];
          } else if (videoUrl.includes('id=')) {
            fileId = videoUrl.split('id=')[1].split('&')[0];
          }
          
          if (fileId) {
            // Convert to direct streaming URL
            videoUrl = `https://drive.google.com/uc?export=download&id=${fileId}`;
          }
        }
        %>
        
        <% if (isExternal) { %>
          <!-- External Link - Try to embed -->
          <% if (videoUrl.includes('drive.google.com')) { %>
            <!-- Google Drive Video -->
            <iframe 
              src="https://drive.google.com/file/d/<%= videoUrl.includes('/file/d/') ? videoUrl.split('/file/d/')[1].split('/')[0] : videoUrl.split('id=')[1].split('&')[0] %>/preview" 
              width="100%" 
              height="480" 
              allow="autoplay"
              style="border: none; background: #000;"
            ></iframe>
          <% } else { %>
            <!-- Other External Links -->
            <video controls autoplay style="width: 100%; max-height: 500px;">
              <source src="<%= videoUrl %>" type="video/mp4">
              Browser Anda tidak mendukung video player.
            </video>
            <div style="background: rgba(168, 85, 247, 0.1); padding: 1rem; margin-top: 1rem; border-radius: 8px; text-align: center;">
              <p style="color: #aaa; margin-bottom: 0.5rem;">Tidak bisa play? Buka di tab baru:</p>
              <a href="<%= videoUrl %>" target="_blank" class="btn btn-primary" style="display: inline-block; padding: 0.8rem 1.5rem;">
                ▶️ Buka Video
              </a>
            </div>
          <% } %>
        <% } else { %>
          <!-- Local Video -->
          <video controls autoplay style="width: 100%; max-height: 500px;">
            <source src="/uploads/<%= anime.videoPath %>" type="video/mp4">
            Browser Anda tidak mendukung video player.
          </video>
        <% } %>
      </div>

      <div class="anime-details">
        <h3>📖 Deskripsi</h3>
        <p><%= anime.description %></p>
        <p class="date">📅 Diupload: <%= anime.uploadDate %></p>
      </div>
    </div>
  </div>

  <div class="bottom-nav">
    <a href="/" class="active">
      <span>🏠</span>
      <span>Beranda</span>
    </a>
    <a href="/upload">
      <span>⬆️</span>
      <span>Upload</span>
    </a>
  </div>

  <!-- WhatsApp Floating Button -->
  <a href="https://wa.me/6282297706541?text=Hai" target="_blank" style="position: fixed; bottom: 80px; right: 20px; z-index: 9999; width: 60px; height: 60px; border-radius: 50%; background: linear-gradient(135deg, #25D366, #128C7E); color: white; display: flex; align-items: center; justify-content: center; font-size: 28px; text-decoration: none; box-shadow: 0 4px 12px rgba(37, 211, 102, 0.5); transition: all 0.3s;" onmouseover="this.style.transform='scale(1.1)'" onmouseout="this.style.transform='scale(1)'">
    💬
  </a>
</body>
</html>
