<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#00a1d6">
  <title>Reset Password - AnimeStream</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .reset-page {
      display: flex;
      min-height: 100vh;
      background: linear-gradient(135deg, #00a1d6, #00b5e5);
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .reset-box {
      background: white;
      border-radius: 16px;
      padding: 3rem;
      max-width: 500px;
      width: 100%;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
    }

    .reset-box h2 {
      color: #333;
      font-size: 2rem;
      margin-bottom: 0.5rem;
      text-align: center;
    }

    .reset-box .subtitle {
      color: #999;
      margin-bottom: 2rem;
      text-align: center;
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-group label {
      display: block;
      margin-bottom: 0.5rem;
      color: #333;
      font-weight: 500;
    }

    .form-group input {
      width: 100%;
      padding: 0.9rem 1rem;
      border: 1px solid #e5e5e5;
      border-radius: 8px;
      font-size: 0.95rem;
      transition: all 0.3s;
    }

    .form-group input:focus {
      outline: none;
      border-color: #00a1d6;
      box-shadow: 0 0 0 3px rgba(0, 161, 214, 0.1);
    }

    .btn-submit {
      width: 100%;
      padding: 1rem;
      background: #00a1d6;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
    }

    .btn-submit:hover {
      background: #00b5e5;
    }

    .back-link {
      text-align: center;
      margin-top: 1.5rem;
      color: #666;
    }

    .back-link a {
      color: #00a1d6;
      text-decoration: none;
    }

    .back-link a:hover {
      text-decoration: underline;
    }

    .alert {
      padding: 1rem;
      border-radius: 8px;
      margin-bottom: 1.5rem;
      font-size: 0.9rem;
    }

    .alert-error {
      background: #fff5f5;
      border: 1px solid #ffe5e5;
      color: #ff6b6b;
    }
  </style>
</head>
<body>
  <div class="reset-page">
    <div class="reset-box">
      <h2>🔐 Reset Password</h2>
      <p class="subtitle">Masukkan password baru untuk akun Anda</p>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-error">
          ⚠️ <%= error %>
        </div>
      <% } %>

      <form action="/reset-password" method="POST">
        <input type="hidden" name="token" value="<%= token %>">

        <div class="form-group">
          <label for="password">Password Baru</label>
          <input type="password" id="password" name="password" required placeholder="Minimal 6 karakter">
        </div>

        <div class="form-group">
          <label for="confirmPassword">Konfirmasi Password</label>
          <input type="password" id="confirmPassword" name="confirmPassword" required placeholder="Ulangi password baru">
        </div>

        <button type="submit" class="btn-submit">🔐 Reset Password</button>
      </form>

      <div class="back-link">
        <p><a href="/login">← Kembali ke Login</a></p>
      </div>
    </div>
  </div>
</body>
</html>
