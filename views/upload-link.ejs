<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="theme-color" content="#8a2be2">
  <title>Upload Anime - AnimeStream</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .upload-method-selector {
      display: flex;
      gap: 1rem;
      margin-bottom: 2rem;
      background: rgba(168, 85, 247, 0.1);
      padding: 0.5rem;
      border-radius: 12px;
    }
    
    .method-btn {
      flex: 1;
      padding: 1rem;
      border: 2px solid transparent;
      background: transparent;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.3s;
      font-size: 1rem;
      font-weight: 600;
      color: #666;
    }
    
    .method-btn.active {
      background: linear-gradient(135deg, #a855f7, #c084fc);
      color: white;
      border-color: #a855f7;
    }
    
    .method-btn:hover {
      transform: translateY(-2px);
    }
    
    .upload-section {
      display: none;
    }
    
    .upload-section.active {
      display: block;
    }
    
    .link-input-area {
      background: rgba(168, 85, 247, 0.05);
      border: 2px dashed #a855f7;
      border-radius: 12px;
      padding: 2rem;
      text-align: center;
      margin-bottom: 1.5rem;
    }
    
    .link-input-area .icon {
      font-size: 3rem;
      margin-bottom: 1rem;
    }
    
    .link-input-area input {
      width: 100%;
      padding: 1rem;
      border: 2px solid #a855f7;
      border-radius: 8px;
      font-size: 1rem;
      margin-top: 1rem;
    }
    
    .supported-platforms {
      display: flex;
      gap: 1rem;
      justify-content: center;
      margin-top: 1rem;
      flex-wrap: wrap;
    }
    
    .platform-badge {
      padding: 0.5rem 1rem;
      background: rgba(168, 85, 247, 0.1);
      border-radius: 20px;
      font-size: 0.85rem;
      color: #a855f7;
      font-weight: 600;
    }
    
    .info-box {
      background: linear-gradient(135deg, rgba(168, 85, 247, 0.1), rgba(192, 132, 252, 0.1));
      border-left: 4px solid #a855f7;
      padding: 1rem;
      border-radius: 8px;
      margin-bottom: 1.5rem;
    }
    
    .info-box h4 {
      color: #a855f7;
      margin-bottom: 0.5rem;
    }
    
    .info-box p {
      color: #666;
      font-size: 0.9rem;
      line-height: 1.6;
    }
    
    .info-box ul {
      margin-top: 0.5rem;
      padding-left: 1.5rem;
      color: #666;
      font-size: 0.9rem;
    }
  </style>
</head>
<body class="upload-page">
  <!-- Navbar -->
  <nav class="navbar">
    <div class="container">
      <div class="nav-left">
        <a href="/" class="logo">🎬 AnimeStream</a>
        <div class="nav-menu">
          <a href="/" class="nav-link">Beranda</a>
          <a href="/upload" class="nav-link active">Upload</a>
          <% if (user) { %>
            <a href="/dashboard" class="nav-link">Dashboard</a>
          <% } %>
        </div>
      </div>

      <div class="nav-right">
        <% if (user) { %>
          <div class="user-menu">
            <div class="user-avatar"><%= user.username.charAt(0).toUpperCase() %></div>
            <span class="user-name"><%= user.username %></span>
          </div>
          <a href="/logout" class="btn-logout" style="margin-left: 1rem; padding: 0.5rem 1rem; background: #ff4757; color: white; border-radius: 8px; text-decoration: none; font-size: 0.9rem;">Keluar</a>
        <% } else { %>
          <a href="/login" class="btn-login">Masuk</a>
        <% } %>
      </div>
    </div>
  </nav>

  <!-- Upload Container -->
  <div class="container">
    <div class="upload-container">
      <!-- Header -->
      <div class="upload-header">
        <h2>📤 Upload Anime Baru</h2>
        <p>Bagikan anime favorit kamu dengan komunitas</p>
      </div>

      <!-- Upload Method Selector -->
      <div class="upload-method-selector">
        <button type="button" class="method-btn active" onclick="switchMethod('link')">
          🔗 Upload dengan Link
        </button>
        <button type="button" class="method-btn" onclick="switchMethod('file')">
          📁 Upload File
        </button>
      </div>

      <div class="upload-body">
        <!-- Link Upload Section -->
        <div id="linkSection" class="upload-section active">
          <div class="info-box">
            <h4>📦 Upload dengan Link (Recommended!)</h4>
            <p><strong>Gratis Unlimited!</strong> Upload video kamu ke Terabox/Google Drive, lalu paste link di sini.</p>
            <ul>
              <li><strong>Terabox:</strong> 1TB gratis per akun!</li>
              <li><strong>Google Drive:</strong> 15GB gratis</li>
              <li><strong>Mega:</strong> 20GB gratis</li>
            </ul>
          </div>

          <form action="/upload-link" method="POST" id="linkUploadForm">
            <!-- Link Input -->
            <div class="form-group">
              <label>🔗 Link Video</label>
              <div class="link-input-area">
                <div class="icon">🎬</div>
                <h3>Paste Link Video Kamu</h3>
                <p style="color: #999; margin-top: 0.5rem;">Upload video ke Terabox/Google Drive, lalu paste link share di sini</p>
                <input type="url" id="videoLink" name="videoLink" required placeholder="https://terabox.com/s/xxxxx atau https://drive.google.com/file/d/xxxxx">
                <div class="supported-platforms">
                  <span class="platform-badge">📦 Terabox</span>
                  <span class="platform-badge">📁 Google Drive</span>
                  <span class="platform-badge">💾 Mega</span>
                  <span class="platform-badge">☁️ OneDrive</span>
                  <span class="platform-badge">📂 Dropbox</span>
                </div>
              </div>
            </div>

            <!-- Anime Info -->
            <div class="form-group">
              <label for="title">🎬 Judul Anime</label>
              <input type="text" id="title" name="title" required placeholder="Contoh: Naruto Shippuden">
            </div>

            <div class="form-group">
              <label for="episode">📺 Episode</label>
              <input type="text" id="episode" name="episode" required placeholder="Contoh: Episode 1">
            </div>

            <div class="form-group">
              <label for="category">📂 Kategori</label>
              <select id="category" name="category">
                <option value="action">Action</option>
                <option value="adventure">Adventure</option>
                <option value="comedy">Comedy</option>
                <option value="drama">Drama</option>
                <option value="fantasy">Fantasy</option>
                <option value="romance">Romance</option>
                <option value="sci-fi">Sci-Fi</option>
                <option value="slice-of-life">Slice of Life</option>
              </select>
            </div>

            <div class="form-group">
              <label for="genre">🏷️ Genre (pisahkan dengan koma)</label>
              <input type="text" id="genre" name="genre" placeholder="Contoh: Action, Adventure, Shounen">
            </div>

            <div class="form-group">
              <label for="description">📝 Deskripsi</label>
              <textarea id="description" name="description" rows="5" required placeholder="Ceritakan tentang anime ini..."></textarea>
            </div>

            <!-- Submit Button -->
            <button type="submit" class="btn btn-primary" style="width: 100%; padding: 1rem; font-size: 1.1rem; font-weight: 600;">
              🚀 Upload Sekarang
            </button>
          </form>
        </div>

        <!-- File Upload Section (Original) -->
        <div id="fileSection" class="upload-section">
          <div class="info-box">
            <h4>⚠️ Upload File (Terbatas)</h4>
            <p>Upload langsung ke server. Video akan hilang saat server restart.</p>
            <p><strong>Recommended:</strong> Pakai "Upload dengan Link" untuk storage unlimited!</p>
          </div>
          
          <p style="text-align: center; color: #999; padding: 2rem;">
            Fitur upload file masih menggunakan ephemeral storage.<br>
            <strong>Gunakan "Upload dengan Link" untuk hasil terbaik!</strong>
          </p>
        </div>
      </div>
    </div>
  </div>

  <script>
    function switchMethod(method) {
      // Update buttons
      document.querySelectorAll('.method-btn').forEach(btn => {
        btn.classList.remove('active');
      });
      event.target.classList.add('active');
      
      // Update sections
      document.querySelectorAll('.upload-section').forEach(section => {
        section.classList.remove('active');
      });
      
      if (method === 'link') {
        document.getElementById('linkSection').classList.add('active');
      } else {
        document.getElementById('fileSection').classList.add('active');
      }
    }
    
    // Form validation
    document.getElementById('linkUploadForm').addEventListener('submit', function(e) {
      const link = document.getElementById('videoLink').value;
      
      if (!link) {
        e.preventDefault();
        alert('❌ Link video harus diisi!');
        return;
      }
      
      // Basic URL validation
      try {
        new URL(link);
      } catch (error) {
        e.preventDefault();
        alert('❌ Link tidak valid! Pastikan link dimulai dengan http:// atau https://');
        return;
      }
    });
  </script>

  <!-- WhatsApp Floating Button -->
  <a href="https://wa.me/6282297706541?text=Hai" target="_blank" style="position: fixed; bottom: 20px; right: 20px; z-index: 9999; width: 60px; height: 60px; border-radius: 50%; background: linear-gradient(135deg, #25D366, #128C7E); color: white; display: flex; align-items: center; justify-content: center; font-size: 28px; text-decoration: none; box-shadow: 0 4px 12px rgba(37, 211, 102, 0.5); transition: all 0.3s;" onmouseover="this.style.transform='scale(1.1)'" onmouseout="this.style.transform='scale(1)'">
    💬
  </a>
</body>
</html>
